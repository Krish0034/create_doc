Flutter doc:

1. flutter packages pub run build_runner build --delete-conflicting-outputs.

2. sudo gem install cocoapods
3. pod install

4. Android build release link on https://wetransfer.com/

5. IOS build release on link https://www.installonair.com

6. keytool -exportcert -alias androiddebugkey -keystore ~/.android/debug.keystore | openssl sha1 -binary | openssl base64

7. Build release cmd:-> Uske line Android studio ke terminal me flutter clean then  flutter pub get then flutter build apk --release

8. SHA KEY GENERATOR CMD:
Go to the project folder in the terminal.

Mac:  keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android
Flutter doc:

1. flutter packages pub run build_runner build --delete-conflicting-outputs.

2. sudo gem install cocoapods
3. pod install

4. Android build release link on https://wetransfer.com/

5. IOS build release on link https://www.installonair.com

6. keytool -exportcert -alias androiddebugkey -keystore ~/.android/debug.keystore | openssl sha1 -binary | openssl base64

7. Build release cmd:-> Uske line Android studio ke terminal me flutter clean then  flutter pub get then flutter build apk --release

8. SHA KEY GENERATOR CMD:
Go to the project folder in the terminal.

Mac:  keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android

Windows:  keytool -list -v -keystore "\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android

Linux:  keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android


create cli in project
    1. create project on firebase
    2. npm install -g firebase-tools
    3. dart pub global activate flutterfire_cli
    4. firebase login
    5. flutterfire configure
Windows:  keytool -list -v -keystore "\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android

Linux:  keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android


create cli in project
    1. create project on firebase
    2. npm install -g firebase-tools
    3. dart pub global activate flutterfire_cli
    4. firebase login
    5. flutterfire configure